populate .sequelizerc
add config/database.json file
sequelize-cli init

npx sequelize-cli db:create

db.sequelize.authenticate to check for connectivity

- or maybe it will automatically log something when connected
